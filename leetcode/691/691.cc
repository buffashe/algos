#include <cstdio>
#include <cstdlib>
#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

class Solution
{
public:
    int minStickers(vector<string> &stickers, string target)
    {
        /*
        int letterAvail['z' - 'a' + 1] = {0};

        for (char c : target)
        {
            if (--letterAvail[c - 'a'] < -stickers.size())
            {
                return -1;
            }
        }

        while (true)
        {
        }
        */
    }
};

int main()
{
    Solution s;

    return 0;
}
